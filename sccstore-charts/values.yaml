# Default values for sccstore-charts.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
global:
  database: 
    service: pg-service
ui:
  enabled: true
  image:
    repository: registry-1.docker.io/domenicopiol
  route:
    host: apps.ocp001.rhlab.ch

pcomplaints:
  enabled: true
qserverless:
  enabled: true
database:
  enabled: true