# Default values for sccstore-serverless.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: registry-1.docker.io/domenicopiol
  name: sccstore-serverless
  tag: latest
