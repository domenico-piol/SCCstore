curl -L --fail "https://hub.docker.com/v2/repositories/domenicopiol/sccstore-ui/tags" | jq '.results[].images |= sort_by(.last_pushed) | del(.results[] | select(.name=="latest") ) | .results[0].name' | sed -e 's/^"//' -e 's/"$//'

helm upgrade  sccstore sccstore-charts --set ui.image.tag=$(./getLatestTag sccstore-ui) --dry-run --debug



oc adm drain host05.rhlab.ch --ignore-daemonsets --force --grace-period=30 --delete-emptydir-data

oc adm uncordon host05.rhlab.ch